---
import Base from '../../layouts/Base.astro';
import diaryData from '../../data/diary.json';

export async function getStaticPaths() {
  return diaryData.diary
    .filter(d => !d.isDraft)
    .map(entry => ({
      params: { id: entry.id },
      props: { entry },
    }));
}

const { entry } = Astro.props;
---
<Base title={`${entry.title} · 不二`} description={entry.excerpt}>
  <a href="/diary" class="inline-flex items-center gap-1 text-pink-400 hover:text-pink-300 text-sm mb-6">
    ← 返回日记
  </a>
  <article class="bg-gray-900 rounded-xl border border-gray-800 p-8">
    <div class="mb-6 pb-6 border-b border-gray-800">
      <span class="text-xs text-pink-400 mb-1 block">{entry.date} · {entry.category}</span>
      <h1 class="text-3xl font-bold mb-3">{entry.title}</h1>
      <div class="flex gap-2 flex-wrap">
        {entry.tags.map(tag => (
          <span class="px-2 py-0.5 bg-gray-800 rounded text-xs text-gray-400">{tag}</span>
        ))}
        {entry.isMilestone && <span class="px-2 py-0.5 bg-yellow-900/30 rounded text-xs text-yellow-400">🏆 里程碑</span>}
      </div>
    </div>
    <div class="text-gray-300 leading-relaxed whitespace-pre-wrap">{entry.content}</div>
  </article>
</Base>
